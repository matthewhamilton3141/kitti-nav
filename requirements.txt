numpy>=1.23.0
opencv-python>=4.8.0        # ORB front-end + stereo matching
pykitti>=0.3.1              # KITTI raw loader (MIT, utiasSTARS) — see ATTRIBUTION.md
matplotlib>=3.7.0           # trajectory / BEV plots
pytest>=7.4.0

# RL only — needed to train/evaluate the learned planner, not to run the rest of the repo.
# The environment core (src/kitti_nav/nav_env.py) is pure NumPy and tests without these.
gymnasium>=0.29             # confined to src/kitti_nav/nav_gym.py
stable-baselines3>=2.0      # PPO (MIT, DLR-RM) — see ATTRIBUTION.md
torch>=2.0                  # policy network; CPU is faster than GPU for these tiny batches
